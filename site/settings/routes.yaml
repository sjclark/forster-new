taxonomies:
  tags: '/blog/tags/{slug}'

routes:
  /blog/tags: blog/taxonomies
  /search: search
  /users: user/index
  '/users/{username}': user/profile
  /account: user/account
  /login: auth/login
  /register: auth/register
  /forgot-password: auth/password-forgot
  /reset-password: auth/password-reset
  /sitemap:
    template: sitemap
    layout: sitemap
    content_type: xml

collections:
  stories: '/stories/{slug}'
  policies: /policy/{year}/{slug}
